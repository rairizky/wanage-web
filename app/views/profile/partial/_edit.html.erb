<%= form_with model: @get_user, url: profile_update_path, html: { multipart: true }, local: true do |f| %>
<div class="form-row">
    <div class="form-group col-md-4">
        <label class="font-weight-semibold">Avatar:</label>
        <%= f.file_field :avatar, class: @get_user.errors[:avatar].any? ? 'form-control is-invalid' : 'form-control', placeholder: '08xxx' %>
        <% if @get_user.errors[:avatar].any? %>
            <div class="invalid-feedback">
                <%= @get_user.errors[:avatar].first %>
            </div>
        <% end %>
    </div>
</div>
<hr>
<div class="form-row">
    <div class="form-group col-md-6">
        <label class="font-weight-semibold">Name:</label>
        <%= f.text_field :name, class: 'form-control', placeholder: 'name...', value: @user.name.titleize, disabled: true %>
    </div>
    <div class="form-group col-md-6">
        <label class="font-weight-semibold">Email:</label>
        <%= f.email_field :email, class: 'form-control', placeholder: 'mail@example.com', value: @user.email, disabled: true %>
    </div>
</div>
<div class="form-row">
    <div class="form-group col-md-4">
        <label class="font-weight-semibold">Phone Number:</label>
        <%= f.text_field :phone, class: @get_user.errors[:phone].any? ? 'form-control is-invalid' : 'form-control', placeholder: '08xxx', value: @get_user.phone %>
        <% if @get_user.errors[:phone].any? %>
            <div class="invalid-feedback">
                <%= @get_user.errors[:phone].first %>
            </div>
        <% end %>
    </div>
    <div class="form-group col-md-4">
        <label class="font-weight-semibold">Date of Birth:</label>
        <%= f.date_field :dob, class: @get_user.errors[:dob].any? ? 'form-control is-invalid datepicker-input' : 'form-control datepicker-input', placeholder: 'date of birth...', value: @get_user.dob %>
        <% if @get_user.errors[:dob].any? %>
            <div class="invalid-feedback">
                <%= @get_user.errors[:dob].first %>
            </div>
        <% end %>
    </div>
</div>
<div class="form-row">
    <div class="form-group col-md-12">
        <label class="font-weight-semibold" for="address">Address:</label>
        <%= f.text_area :address, class: @get_user.errors[:address].any? ? 'form-control is-invalid' : 'form-control', placeholder: 'address...', value: @get_user.address %>
        <% if @get_user.errors[:address].any? %>
            <div class="invalid-feedback">
                <%= @get_user.errors[:address].first %>
            </div>
        <% end %>
    </div>
</div>
<div class="form-row">
    <div class="form-group col-md-12 d-flex justify-content-end">
        <% if policy(@get_user).update? %>
            <button class="btn btn-primary">Update</button>
        <% end %>
    </div>
</div>
<% end %>